<view bindtap="handleTap" class="i-cell {{ isLastCell ? 'i-cell-last' : '' }} {{ isLink ? 'i-cell-access' : '' }} i-class">
    <view class="i-cell-icon">
        <slot name="icon"></slot>
    </view>
    <view class="i-cell-bd">
        <view style="font-size: {{titleSize}}px" wx:if="{{ title }}" class="i-cell-text">{{ title }}</view>
        <view style="font-size: {{labelSize}}px" wx:if="{{ label }}" class="i-cell-desc">{{ label }}</view>
        <slot></slot>
    </view>
    <view catchtap="navigateTo" class="i-cell-ft">
        <block wx:if="{{value}}">{{ value }}</block>
        <block wx:else>
            <slot name="footer"></slot>
        </block>
    </view>
</view>